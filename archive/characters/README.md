# Archived bundled characters

This directory keeps retired character source assets for possible future reuse.
Files here are intentionally excluded from the desktop package and are not shown
as built-in characters.

- `silver-hood-avatar/`: Luna (ルナ), retired from the built-in library in favor of Towa (トワ).

